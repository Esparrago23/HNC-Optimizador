# ─────────────────────────────────────────────
# HNC-Optimizador — Dependencias Python
# ─────────────────────────────────────────────
# Instalar con:
#   pip install -r requirements.txt
#
# Python recomendado: 3.10 o superior

# Servidor web
flask>=2.3.0

# Datos y ETL
pandas>=2.0.0
openpyxl>=3.1.0      # lectura/escritura de archivos .xlsx
xlrd>=2.0.0          # soporte adicional para .xls legacy (opcional)
